# ====================================================
# FREEQUO BACKEND ENVIRONMENT VARIABLES
# ====================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# ====================================================

# Server Configuration
PORT=5000
NODE_ENV=development

# ====================================================
# MongoDB Atlas (Required)
# ====================================================
# Create free cluster at: https://cloud.mongodb.com
# Connection string format: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>
MONGODB_URI=mongodb://localhost:27017/freequo

# ====================================================
# JWT Authentication (Required)
# ====================================================
# Use a strong, random secret in production
# Generate one: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# ====================================================
# CORS Configuration
# ====================================================
FRONTEND_URL=http://localhost:5173

# ====================================================
# Cloudinary - File Uploads (Optional but recommended)
# ====================================================
# Create free account at: https://cloudinary.com
# Get credentials from Dashboard → Account Details
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ====================================================
# Razorpay - Payments (Optional)
# ====================================================
# Create test account at: https://dashboard.razorpay.com/signup
# Get keys from: Settings → API Keys → Generate Test Key
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# ====================================================
# SMTP - Email (Optional)
# ====================================================
# For production emails, configure SMTP
# Gmail example (use App Password, not regular password):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=Freequo <noreply@freequo.com>

# ====================================================
# Payment Mode
# ====================================================
# 'mock' for testing without real payments
# 'live' when Razorpay is configured
PAYMENT_MODE=mock
